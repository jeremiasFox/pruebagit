#hola mundogu
"""
git add para guardar en el repositorio local

git commit -m "test_git.py" subir al repositorio local

git push para subir cambios al repositorio online

git pull para bajar los cambios del repositorio 

git status (ver el estado actual)
git diff (ver que cambió dentro de cada archivo)
git add <nombre del archivo> (instancia el archivo para hacer un commit)
git commit -m "<mensaje>" (hacen el commit)
git push (suben su commit al repo)
git pull (traen los commits del resto del repo a su compu)



"""
